use reverse_string::*;
fn main() {
    let original = "hello apophis is coming";
    let reversed = rev_str(original);
    println!("Original: {}", original);
    println!("Reversed: {}", reversed);
}
